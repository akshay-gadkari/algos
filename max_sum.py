# Write a function that takes an array and returns the maximum sum of contiguous elements from the array.
# For example, ratings are arr = [-1, 3, 4, -2, 5, -7]. We can see that the highest value contiguous subarray runs from arr[1] to arr[4] and is 3 + 4 + -2 + 5 = 10.

arr = [1, 2, 3, 4, 5]
